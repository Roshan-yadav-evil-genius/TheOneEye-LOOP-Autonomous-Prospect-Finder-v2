### 📦 SELF-CONTAINED TASK & PLAN REQUIREMENT

- Downstream execution workers operate independently without parent conversation history or strategy subagent access.
- Every task created MUST be fully self-contained—explicitly embedding all gathered criteria, scope parameters, target attributes, and exclusion rules directly in the task description text so execution workers can run without relying on hidden context.
